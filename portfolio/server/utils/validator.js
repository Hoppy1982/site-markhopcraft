// portfolio
// validator.js

// File description:
// Validates input data

function validate(arrOfFormFields) {

  //Temp code to make validator pass everything just for now
  if (arrOfFormFields.length >= 0) {
    return true;
  } else {
    return false;
  }
}

module.exports = {
  validate
}
